# Analyze spatial patterns

Decide what the map should reveal: clusters, regions, routes, account territories, supplier risk, coverage gaps, or other spatial patterns. Define layers and filters that help the user inspect those patterns.

Treat ambiguous locations carefully. Prefer explicit caveats over false precision.
