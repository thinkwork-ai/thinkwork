# Analyze pipeline and account risk

Superseded by the fast CRM dashboard flow. New runs should not create a separate analysis phase; compute only the compact metrics needed by `references/discover.md` and build the artifact in `references/produce.md`.
