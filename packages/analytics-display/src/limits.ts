export const analyticsDisplayLimits = {
  maxRows: 500,
  maxTableColumns: 20,
  maxElements: 12,
  maxChartPoints: 200,
  maxSeries: 5,
  maxLabelLength: 120,
  maxStringValueLength: 1_000,
  maxSerializedPayloadBytes: 100_000,
  maxSummaryLines: 6,
} as const;
