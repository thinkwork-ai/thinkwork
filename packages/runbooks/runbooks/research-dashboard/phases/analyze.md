# Synthesize findings

Cluster the evidence into themes, comparisons, decision criteria, risks, and confidence levels. Preserve disagreements and gaps instead of smoothing them away.

The output should define the dashboard's point of view: what users need to inspect, compare, filter, and verify.
