# Validate map result

Check mapped entities against the source evidence. Confirm that approximate, inferred, missing, or ambiguous locations are labeled appropriately.

Verify that the map artifact matches the user's requested geography and does not overstate unsupported location precision.
