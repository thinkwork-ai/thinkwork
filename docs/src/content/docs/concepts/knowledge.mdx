---
title: Company Brain
description: The harness-owned context substrate for ThinkWork agents — memory, compiled pages, knowledge bases, workspace files, and approved tools behind one inspectable layer.
---

import { Card, CardGrid, Aside } from '@astrojs/starlight/components';

Company Brain is the context substrate agents use before they act. It is not a single database, vector index, or chat-history trick. It is the ThinkWork-owned layer that decides what an agent should know for the current turn, where each fact came from, how fresh it is, and whether it is durable enough to carry forward.

The clean model is:

- **Threads** keep the complete record of work.
- **Company Brain** assembles the context that should matter now.
- **Agents** reason and act using that assembled context.

That distinction matters. Threads answer "what happened?" Company Brain answers "what should the agent know before it responds or takes action?"

## What It Contains

Company Brain is an umbrella over several source families. Operators see them together in Admin because they all affect what an agent can know, but each source keeps its own contract and provenance.

<CardGrid>
  <Card title="Memory" icon="seti:bookmark">
    Durable facts, preferences, and observations retained from prior work. Hindsight and AgentCore Memory are adapter implementations below the ThinkWork memory contract.
  </Card>
  <Card title="Pages" icon="graph">
    Compiled Company Brain pages: entity, topic, and decision pages distilled from memory records and source citations.
  </Card>
  <Card title="Knowledge Bases" icon="open-book">
    Operator-curated documents backed by Bedrock Knowledge Bases and Aurora pgvector. Best for policies, runbooks, product docs, and other authored source material.
  </Card>
  <Card title="Workspace Files" icon="file">
    Permissioned files from an agent workspace, template workspace, or tenant defaults.
  </Card>
  <Card title="Approved MCP Tools" icon="setting">
    Read-only, search-safe external tools that an admin has explicitly allowed to participate in context lookup.
  </Card>
  <Card title="Source Routing" icon="search">
    The internal `query_context` contract that fans out to eligible sources and returns normalized hits plus per-source status.
  </Card>
</CardGrid>

## Why It Exists

Most agent systems expose memory, RAG, workspace search, and external search as separate tools. That makes agents choose implementation details mid-turn and makes operators debug the wrong layer: was the answer bad because the memory engine missed, the KB was stale, or the agent never called the right tool?

Company Brain puts those sources behind one product boundary:

- operators can inspect and test the sources together;
- agents can ask for context without hard-coding every backend;
- every hit carries provenance, source family, rank, freshness, and provider status;
- backend choices can change without changing the product story.

## How a Turn Uses Company Brain

At turn time, Company Brain can assemble context from recent thread history, memory recall, compiled pages, knowledge-base chunks, workspace files, and approved MCP tools. The router ranks and budgets those results before the model sees them.

```
User message arrives in a thread
        │
        ▼
  Company Brain source routing
        │
        ├─ Recent thread history
        ├─ Memory recall
        ├─ Compiled pages
        ├─ Knowledge-base chunks
        ├─ Workspace files
        └─ Approved MCP search tools
        │
        ▼
  Assembled context → model/runtime
        │
        ▼
  Response + tool calls → thread
```

Every source is optional. A focused support bot may only need a knowledge base. A long-running sales agent may use memory, compiled customer pages, workspace files, and approved operational search tools.

## Trust and Provenance

Company Brain treats source trust as a product concern, not a prompt afterthought.

- **Authored knowledge** comes from operator-managed documents and keeps file/chunk citations.
- **Memory** comes from prior work and keeps adapter/source references.
- **Compiled pages** summarize many source facts and keep page-section provenance.
- **External MCP hits** are transient unless a separate workflow promotes them.
- **Operational facets** can be live and fresh, but should not be silently converted into durable narrative facts.

<Aside type="note">
The internal Context Engine service and `query_context` tool are still the developer contract behind Company Brain source routing. Product-facing docs use Company Brain; API reference docs still name the exact contract developers call.
</Aside>

## Admin Surface

In Admin, Company Brain lives at `/knowledge` and has four tabs:

| Tab | Purpose |
|---|---|
| Memory | Browse, search, edit, and visualize retained memory records |
| Pages | Inspect compiled Company Brain pages and graph/search output |
| Knowledge Bases | Manage Bedrock KBs, document uploads, and sync status |
| Sources | Configure and verify the provider layer used by `query_context` |

The route path still says `/knowledge` for compatibility, but the visible module name is Company Brain.

## Related Pages

- [Sources and Knowledge Bases](/concepts/knowledge/document-knowledge/) — authored document sources and Bedrock KBs
- [Memory Facet](/concepts/knowledge/memory/) — retained facts and memory backends
- [Compiled Pages](/concepts/knowledge/compounding-memory/) — durable page distillation
- [Compile Pipeline](/concepts/knowledge/compounding-memory-pipeline/) — how memories become pages
- [Page Model](/concepts/knowledge/compounding-memory-pages/) — sections, aliases, links, mentions, and sources
- [Source Routing](/concepts/knowledge/retrieval-and-context/) — how `query_context` assembles source hits
- [Admin — Company Brain](/applications/admin/knowledge/) — operator workflows
