---
title: Admin — Memory
description: "The legacy overview for the Settings Memory operator surface."
---

The Settings Memory page is the operator hub for durable memory and lazy-loaded context. It combines AgentCore memory records, compiled pages, workspace/source routing, and provider verification under one tabbed module.

**Route:** `/settings/memory`

**Tabs:** Memory, Sources, Wiki

Older docs and redirects may still refer to `/knowledge`. The visible product/module name is Memory, and the canonical operator route is `/settings/memory`.

## Why It Exists

Agents draw context from several places:

- durable requester memory in Bedrock AgentCore Memory;
- compiled pages from the page compile pipeline;
- workspace files scoped to an agent, Space, or template;
- approved MCP context tools and other lazy-loaded external context.

Memory keeps source inspection and source routing close together. Operators can look at the raw source, then run the same backend query path agents use through `query_context`.

## Tabs

| Tab             | Purpose                                                                          |
| --------------- | -------------------------------------------------------------------------------- |
| Memory          | Browse, search, and edit retained AgentCore memory records                        |
| Wiki            | Inspect compiled pages and page search output                                    |

The old standalone routes redirect into the matching tab:

| Old route          | Current route                      |
| ------------------ | ---------------------------------- |
| `/memory`          | `/settings/memory`                 |
| `/wiki`            | `/settings/memory/wiki`            |

## Sources Query Test

The Sources tab includes a safe query harness for operator verification. It is intended for dogfooding source behavior before testing through a full agent chat.

The harness controls are:

- **Sources** — multi-select dropdown for provider families such as memory, compiled pages, workspace files, and approved MCP tools.
- **Agent target** — optional agent picker used when workspace files or agent-scoped attachments should participate in the test.
- **Search** — natural-language query field.

Results show provider statuses and top hits side by side. Clicking a hit opens a formatted markdown preview plus the raw structured hit. Clicking **View full result** opens the complete `query_context` response. Provider failures should be local: a slow or skipped external provider should not hide successful page or workspace hits.

## Template Configuration

Agent Templates expose ThinkWork Brain/context tools alongside Code Sandbox, Browser Automation, Web Search, and Send Email. Enabling Context Engine injects the platform-owned `query_context` tool for external context; durable memory remains the direct `recall` tool over AgentCore Memory.

Template configuration can choose which tenant-approved external sources participate by default. AgentCore Memory is the durable substrate rather than one interchangeable Context Engine adapter; the operator-facing path is Memory.

## Operational Guidance

- Use the Sources tab first when debugging external provider behavior. It gives provider-level latency, hit counts, skipped states, and partial failures without model/tool-selection noise.
- Select an agent target when testing workspace files. Workspace search needs the agent workspace scope to find files such as `USER.md`.
- Use Pages when you want the durable compiled view of what an agent has learned; use Memory when you need the raw retained records.
- Use workspace files for authored, operator-curated documents a Space or agent should be able to cite.

## Related Pages

- [Memory](/concepts/knowledge/) — product model and source families
- [Memory Context API](/api/context-engine/) — shared `query_context` contract and runtime tool behavior
- [Retained Memory](/applications/admin/memory/) — details of the AgentCore-backed Memory tab behavior
- [Tenant Agent](/applications/admin/agents/) — tenant-wide platform-agent workspace
- [Spaces](/applications/admin/spaces/) — Space-scoped workspace, tools, memory, and knowledge context
