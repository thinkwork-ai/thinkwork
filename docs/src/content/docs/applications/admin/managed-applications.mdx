---
title: Managed Applications
description: Operator controls for optional ThinkWork-managed applications such as Cognee Knowledge Graph, Twenty CRM, and Kestra.
---

import { Aside } from "@astrojs/starlight/components";

Managed Applications are optional applications that ThinkWork provisions and
operates inside the same AWS deployment. They are not end-user connector
settings. They are infrastructure controls for operators who can change the
stage deployment through the normal GitHub Actions and Terraform pipeline.

**Surface:** Spaces/Desktop Settings -> General -> Managed Applications  
**Primary files:** `apps/web/src/components/settings/ManagedApplicationsSection.tsx`,
`packages/api/src/graphql/resolvers/core/managedApplications.ts`

## What Lives Here

The first managed applications are:

| Application | Purpose                                           | Settings page appears when                |
| ----------- | ------------------------------------------------- | ----------------------------------------- |
| Cognee      | Knowledge Graph substrate for ontology work       | Deployment status reports Cognee running  |
| Twenty CRM  | Self-hosted CRM runtime managed by ThinkWork      | Deployment status reports Twenty running  |
| Kestra      | Self-hosted orchestration runtime for agent flows | Deployment status reports Kestra retained |

The Managed Applications section is visible only to operators. Members do not
see deployment controls, and app-specific settings pages remain hidden until
deployment status reports the runtime as enabled.

<Aside type="note">
  Managed application actions queue the deploy workflow. They do not mutate
  production infrastructure directly from the UI.
</Aside>

## Lifecycle

ThinkWork exposes three lifecycle actions for retained managed applications:

| Action  | Effect                                                                                  |
| ------- | --------------------------------------------------------------------------------------- |
| Deploy  | Provisions retained infrastructure and starts the app runtime                           |
| Park    | Stops runtime capacity while retaining app data, endpoint wiring, secrets, and MCP path |
| Destroy | Removes runtime infrastructure, retained storage/cache, secrets, DB state, and MCP rows |

The deployed status still resolves to the retained/runtime state:

| Provisioned | Runtime enabled | Meaning                                                      |
| ----------- | --------------- | ------------------------------------------------------------ |
| false       | false           | Never enabled; no retained app resources exist               |
| true        | true            | Runtime is running and the app settings page is visible      |
| true        | false           | Runtime is parked; retained resources and repair path remain |

Parking Twenty stops the server and worker runtime capacity while retaining the
dedicated database, Secrets Manager values, persistent file storage, cache
configuration, DNS path, and re-enable path.

Parking Kestra stops the standalone ECS/Fargate service while retaining flow
definitions, execution history, internal storage, endpoint wiring, Secrets
Manager credentials, and the managed MCP continuity path.

Destroying Twenty is intentionally separate from parking. It queues a
destructive deploy that tears down the Twenty infrastructure and then removes
the dedicated Twenty database, database role, and Twenty Secrets Manager
entries. Use it only when CRM data is no longer needed.

Destroying Kestra is also intentionally separate from parking. It removes the
Kestra runtime, S3 internal storage, dedicated database state, service
credentials, and managed control MCP row/token/configuration. Use it only when
orchestration data and flow history are no longer needed.

Cognee follows the existing optional Knowledge Graph lifecycle. Its deploy
control now lives in Managed Applications; the Knowledge Graph settings page is
for operational details and health once Cognee is running.

## Twenty CRM V1

Twenty CRM v1 is a deployable managed app, not a custom ThinkWork CRM product.
When enabled, Terraform provisions:

- public HTTPS access at the managed CRM host;
- ECS/Fargate server and worker services;
- a dedicated Twenty database and role on the existing Aurora/Postgres
  instance;
- ElastiCache Valkey/Redis-compatible cache;
- persistent file storage and CloudWatch logs;
- Secrets Manager entries for the database URL and Twenty encryption key.

The CRM settings page shows the public URL, stage, region, service names, log
groups, load balancer/target group identifiers, and a `/healthz` connection
test.

## First Admin Setup

Twenty v1 uses Twenty's native first-user setup. After the runtime is deployed,
open the CRM URL from Settings -> CRM and complete Twenty's first workspace
admin flow there.

ThinkWork does not pre-seed CRM users, invites, or workspaces in v1.

## Deferred Follow-Up

ThinkWork SSO/Cognito/Google integration for Twenty is documented as follow-up
work after the smallest deployable release. The CRM settings page should show
that SSO is deferred rather than exposing inactive SSO controls.

Other deferred work includes richer CRM connector workflows, write-policy
design for CRM mutations, and a first-class CRM extension inside ThinkWork.

## Twenty CRM MCP

When Twenty CRM is running, ThinkWork reconciles a system-managed MCP server
named **Twenty CRM** into Settings -> MCP Servers. Operators should not add a
second manual Twenty MCP row. Use the CRM settings page only to install or
repair the managed MCP registration when an already-running stage is missing
the row.

Managed Applications and MCP Servers intentionally split responsibilities:

| Surface                                     | Responsibility                                                        |
| ------------------------------------------- | --------------------------------------------------------------------- |
| Settings -> General -> Managed Applications | Deploy, park, destroy, and recover managed application infrastructure |
| Settings -> CRM                             | Inspect CRM runtime state and repair the managed MCP registration     |
| Settings -> MCP Servers                     | Let each user connect, reconnect, or clear their own Twenty MCP auth  |

Parking Twenty disables runtime availability for the managed MCP connector but
keeps the MCP row and per-user token records so reconnect continuity survives a
redeploy. Destructive Destroy removes the managed MCP row and token material as
part of full CRM cleanup.

When the managed MCP row is installed or repaired, ThinkWork assigns it to the
tenant platform default agent. That makes the connector available to runtime
only after the current user has completed their own OAuth connection.

## Kestra V1

Kestra v1 is a managed orchestration runtime for ThinkWork agents. It is not a
general-purpose host executor. When enabled, Terraform provisions:

- public HTTPS access at the managed orchestration host;
- a standalone ECS/Fargate Kestra service;
- a dedicated Kestra database and role on the existing Aurora/Postgres
  instance;
- encrypted S3 internal storage for Kestra files;
- CloudWatch logs and ALB target health checks;
- Secrets Manager entries for the database password and Kestra UI/API basic
  auth.

The Kestra settings page shows the public URL, retained/runtime state, storage
bucket, database name, service names, log group, load balancer/target group
identifiers, health-check result, and managed MCP registration status.

Kestra v1 supports Fargate-safe orchestration classes: HTTP calls, API
coordination, validation, and control-plane flows that run inside the managed
Kestra service. It does not support Docker socket execution, Docker-in-Docker,
privileged containers, arbitrary host/container execution, or EC2 worker pools.
Agents should validate flows through the control MCP before upserting them.

## Kestra Control MCP

When Kestra is retained, ThinkWork reconciles a system-managed MCP server named
**Kestra Control** into Settings -> MCP Servers. Operators should not register a
second manual control row for the same customer Kestra instance. Use Settings
-> Applications -> Kestra to install or repair the managed row when an
already-retained stage is missing it.

The Kestra control MCP is different from Twenty's per-user OAuth connector:

| Surface                                     | Responsibility                                                        |
| ------------------------------------------- | --------------------------------------------------------------------- |
| Settings -> General -> Managed Applications | Deploy, park, destroy, and recover managed application infrastructure |
| Settings -> Applications -> Kestra          | Inspect Kestra runtime state and repair the managed control MCP row   |
| Settings -> MCP Servers                     | Show the system-managed row and assignment state                      |

ThinkWork stores the Kestra MCP bearer token and Kestra service credential in
Secrets Manager. The database row contains secret references and runtime
metadata, not plaintext credentials. The row is assigned to the tenant platform
default agent so ThinkWork agents can use the curated `/mcp/kestra` tool
surface.

The curated control tools are intentionally narrow:

- `kestra_namespaces_list`
- `kestra_flows_get`
- `kestra_flows_validate`
- `kestra_flows_upsert`
- `kestra_executions_start`
- `kestra_executions_get`
- `kestra_executions_logs`
- `kestra_plugins_search`

By default, write operations are limited to the `thinkwork` namespace prefix
and reject unsupported Docker/host task classes before they reach Kestra.

Do not confuse this control MCP with a public Kestra catalog MCP or docs/plugin
discovery server. Public catalog tools are useful context for finding plugins
and examples; they do not authenticate to, mutate, or operate the customer
Kestra instance. Use catalog context for authoring ideas, then use
`/mcp/kestra` to validate, upsert, execute, and inspect flows in the managed
runtime.

## Deployment Verification

After the deploy workflow finishes, run the managed-app smoke from the repo
root:

```sh
node scripts/smoke/twenty-managed-app-smoke.mjs

SMOKE_ENABLE_TWENTY_MANAGED_APP=1 \
  SMOKE_TENANT_ID=<tenant-id> \
  node scripts/smoke/twenty-managed-app-smoke.mjs
```

Dry-run mode prints the live-mode requirements and exits successfully. Live
mode reads Terraform outputs and, when GraphQL credentials are available,
deployment status. If Twenty is not provisioned, the smoke skips with a clear
message. If Twenty is running, it requires an HTTPS URL and probes the public
`/healthz` endpoint.

After the CRM runtime is healthy and the user has connected Twenty from
Settings -> MCP Servers, run the MCP OAuth smoke:

```sh
node scripts/smoke/twenty-mcp-oauth-smoke.mjs

SMOKE_ENABLE_TWENTY_MCP_OAUTH=1 \
  SMOKE_API_BASE_URL=<api-url> \
  SMOKE_COGNITO_ID_TOKEN=<current-user-id-token> \
  SMOKE_TENANT_ID=<tenant-id> \
  SMOKE_USER_ID=<user-id> \
  SMOKE_AGENT_ID=<agent-id> \
  node scripts/smoke/twenty-mcp-oauth-smoke.mjs
```

Dry-run mode prints the required live-mode inputs. Live mode verifies Twenty
OAuth metadata, the system-managed MCP row, the current user's connected auth
status, and the ThinkWork MCP proxy `tools/list` path. Set
`SMOKE_TWENTY_MCP_CALL=1` plus `SMOKE_TWENTY_USER_EMAIL` or
`SMOKE_TWENTY_WORKSPACE_MEMBER_ID` to run the required
assigned-opportunities tool proof through ThinkWork's runtime credential
injection path:

```sh
SMOKE_ENABLE_TWENTY_MCP_OAUTH=1 \
  SMOKE_TWENTY_MCP_CALL=1 \
  SMOKE_TWENTY_USER_EMAIL=<twenty-user-email> \
  SMOKE_API_BASE_URL=<api-url> \
  SMOKE_COGNITO_ID_TOKEN=<current-user-id-token> \
  SMOKE_TENANT_ID=<tenant-id> \
  SMOKE_USER_ID=<user-id> \
  SMOKE_AGENT_ID=<agent-id> \
  node scripts/smoke/twenty-mcp-oauth-smoke.mjs
```

For Kestra, run the managed-app smoke first:

```sh
node scripts/smoke/kestra-managed-app-smoke.mjs

SMOKE_ENABLE_KESTRA_MANAGED_APP=1 \
  SMOKE_TENANT_ID=<tenant-id> \
  node scripts/smoke/kestra-managed-app-smoke.mjs
```

Dry-run mode prints live requirements. Live mode reads Terraform outputs and,
when GraphQL credentials are available, deployment status and health. If Kestra
is unprovisioned or parked, the smoke exits with an explicit skip. If Kestra is
running, it requires an HTTPS URL and verifies the public endpoint returns
either success or an authentication challenge.

Then run the Kestra control MCP smoke:

```sh
node scripts/smoke/kestra-control-mcp-smoke.mjs

SMOKE_ENABLE_KESTRA_CONTROL_MCP=1 \
  SMOKE_API_BASE_URL=<api-url> \
  SMOKE_KESTRA_MCP_BEARER=<tenant-control-mcp-token-or-service-secret> \
  SMOKE_TENANT_ID=<tenant-id> \
  node scripts/smoke/kestra-control-mcp-smoke.mjs
```

Live mode talks directly to `POST /mcp/kestra`, verifies the managed MCP row
when GraphQL credentials are available, lists the curated tools, validates a
safe flow, validates that an unsupported Docker task is rejected, and can
upsert/start/poll a safe `thinkwork.smoke` flow. Set
`SMOKE_KESTRA_CONTROL_MUTATE=0` for validation-only live runs.

## Related Pages

- [Admin — Settings](/applications/admin/settings/) — read-only deployment metadata
- [Admin — Kestra](/applications/admin/kestra/) — Kestra-specific operator runbook
- [Desktop App](/applications/desktop/) — installed Spaces app that hosts Settings
- [Business Ontology Operations](/guides/business-ontology-operations/) — Cognee substrate guidance
- [Deploy Configuration](/deploy/configuration/) — Terraform configuration reference
- [MCP Tools](/concepts/mcp-tools/) — managed MCP registration and per-user OAuth model
