# THINK-213 completion run — progress file

Working protocol: implement sub-issues one at a time (214 → 215 → 216 → 217 → 218), fresh worktree per issue off origin/main, PR squash auto-merge, watch the deploy carrying the commit, live-verify on dev, Linear completion comment + Done, worktree cleanup, handoff comment to THINK-213, /compact between issues. Re-orient ONLY from Linear + this file after each compaction. Parent plan: `docs/plans/2026-07-07-002-feat-canonical-run-model-plan.md`.

## Status

- [x] **THINK-214** — Done 2026-07-07. PR #3515 (`89da3d05a`), deploy 28902595087 green. Full step taxonomy (agent/routine/tool/approval/wait/http/emit_event) + `{{ trigger/run.input/steps.<id>.output }}` mapping language + per-kind ThinkWork-terms validation in `packages/agent-loops-core/src/workflow-definition.ts`. Ship-inert: `planNextStep` → `unsupported_step` directive; dispatch Lambda fails such runs with `step_kind_not_executable` (probe run `0d31086f-1244-409d-8ce2-a3f34bfc2f91` on dev). Interpreter E2E rerun: both cases succeeded.
- [x] **THINK-215** — Done 2026-07-07. PRs #3516 (`a1385dd3f`) + fix #3517 (`e3282b0ac`); deploys green. execute_step directive + ASL state runs routine (routine-exec-git unchanged)/http/emit_event in-Lambda; approval STEP parks via task token (record_approval branches step-vs-policy); dispatch-time template resolution; step outputs land as `workflow_evidence` `step_output` rows; dispatch Lambda timeout 600s. `tool` still fails cleanly — NO headless tenant-tool runner exists (admin-ops-mcp is key-authed admin JSON-RPC); explicit follow-up. Fan-out deliberately absent from v1 taxonomy. Live-only bug fixed: workflow-resume must find approval tokens by purpose (not fixed step_id) — E2E now four cases, all green (taxonomy run f2e9ac8c-6fb1-41e5-9e05-7521abfcba3d).
- [x] **THINK-216** — Done 2026-07-07. PR #3518 (`a51633b7f`); migration 0225 applied to dev; ALL 7 dev automations migrated (118 runs imported, schedule + 2 webhooks repointed, loops archived). AE3 proven live (frozen agent_loop_schedule payload → job-trigger row-priority redirect → workflow run 842693fa succeeded via routine step; zero new agent_loop_runs). Webhook proven (202+runId, run 542ef566 succeeded). Converter = `workflowDefinitionFromAgentLoopVersion`; script = `packages/api/scripts/migrate-agent-loops-to-workflows.mts` (idempotent — RE-RUN at 218 cutover to sweep automations created in the window). GraphQL `agentLoop*` vocabulary lag accepted until 218. Agent-loop code removal + DROPs deferred until after 218. Gotchas: EventBridge payloads frozen at provision (row-priority redirect handles); duplicate step_finished events on agent steps (cosmetic, dedupe in 218's timeline); stale sticky budget_paused flags can mask fires.
- [x] **THINK-217** — Done 2026-07-08. PR #3519 (`b059422f0`). Bridge machinery deleted (lib/n8n-agent-step, bridge+expirer Lambdas terraform-destroyed, HMAC contract, importN8nRoutine, graph-mirroring surface incl. connect/disconnect/discover/import-draft); plugin App UI kept (new n8n-app-data.graphql); live-verified (Lambdas ResourceNotFound; generic webhook 202+runId). CI fixes: canvas in pnpm.neverBuiltDependencies (cold-cache installs); 11 stale plugin-boundary allowlist entries. `n8n_agent_step_runs` physical DROP now unblocked — ride it in the 218 PR (psql-apply + migration file).
- [x] **THINK-218** — DONE 2026-07-08. PR #3523 MERGED (`30b280062`); deploy run 28951751133 in flight. Included 6 rounds of Eric's live UI feedback: header tabs (Memory pattern; PageHeader tabs gained search/active), muted header icon actions, Definition tab = full-height canvas + persistent side card (workflow info default / focused step on node click; routine steps show name+description+module+validated commit with editor jump), detail tabs = Definition|Executions (n8n-style left list + canvas), index tabs = Workflows|Routines|Executions, edit dialog drops the definition editor. GOTCHA: PR went CONFLICTING when main moved → pull_request CI silently stops firing (only CLA runs) — fix = merge origin/main (codegen conflicts: checkout --theirs + regenerate all codegens). Original notes: PR #3523 in CI (auto-merge armed). saveWorkflow mutation (validation-first R4, version supersede, schedule/webhook bindings) + unified three-tab Workflows section (Workflows/Runs/Library), nav retirement + redirects, create/edit dialog, DefinitionStepsPanel (no ASL), step_output evidence + durations. All gates green (web 1982, api 6759, repo tsc). Visually verified on local dev server (port 5174 — 5180 was held by a stale think155 vite; 5174 is Cognito-allowed). Migration sweep re-run --write: "Weekly Pipeline Report" migrated (3 runs). readiness_state=ready stamped in script + backfilled 8 dev rows (Status showed "Unknown"). Source column "Unknown" until first real run — ensureInterpreterBinding is lazy, self-heals. 0226 DROP applied + rides PR. Post-merge: watch deploy, live-verify saveWorkflow create via UI (dialog correctly surfaced pre-deploy schema error inline), Linear Done, cleanup.
- [x] **Dogfood phase** — DONE 2026-07-08. Live evidence: create via UI (wf 51766d02) + manual trigger (run 37fea411 succeeded); scheduled = migrated LastMile hourly fires; approve/deny = THINK-215 E2E + live deny today; loop-to-succeeded = E2E manual-loop via goal_complete; iteration-cap guard = live probe run 1dbd86af (cap → human_needed policy decision w/ tokensUsed → deny → canceled — cap ESCALATES, doesn't hard-fail); migrated automation = AE3 842693fa + webhook 542ef566. UI verified on localhost:5174 (worktree dev server KEPT RUNNING for Eric until desktop-v* canary; worktree cleanup deferred to regroup). Checklist on THINK-213; THINK-219 already closed.

## Standing gotchas

- Deploys change-detect on squash diff; container jobs skip unless `packages/agentcore-pi/**` changes — dispatch deploy.yml manually if needed. AWS region us-east-1.
- Dev DB: secret `thinkwork-dev-db-credentials` (user/pass only) + cluster endpoint `thinkwork-dev-db.cluster-cmfgkg8u8sgf.us-east-1.rds.amazonaws.com:5432/thinkwork`; E2E gated `WORKFLOW_INTERPRETER_E2E=1`, run from `packages/api` with `npx vitest run test/integration/workflow-interpreter --testTimeout=1200000`.
- Live-probe scripts importing @thinkwork/* must run from inside `packages/api` (copy .mts into the dir; tsx outside the workspace can't resolve).
- Goal mode headless works via the pi-coding-agent pnpm patch (#3512) — re-cut on version bump.
- Worktree bootstrap: `pnpm install --prefer-offline`, delete `*.tsbuildinfo`, build database-pg then agent-loops-core. `pnpm format` is broken in worktrees (prettier not a root dep) — use `node_modules/.bin/prettier --write <files>` and NEVER `pnpm add` it.
- One transient E2E double-failure (~10s, setup-stage) was observed once post-deploy and did not reproduce across two subsequent full passes.
