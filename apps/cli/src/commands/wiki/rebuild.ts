/**
 * `thinkwork wiki rebuild` runner. Scaffolded here in Unit 2; full
 * implementation lands in Unit 4.
 */

import type { WikiCliOptions } from "./helpers.js";

export async function runWikiRebuild(_opts: WikiCliOptions): Promise<void> {
	throw new Error("wiki rebuild: implementation lands in Unit 4");
}
