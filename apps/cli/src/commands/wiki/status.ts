/**
 * `thinkwork wiki status` runner. Scaffolded here in Unit 2; full
 * implementation lands in Unit 6.
 */

import type { WikiCliOptions } from "./helpers.js";

export async function runWikiStatus(_opts: WikiCliOptions): Promise<void> {
	throw new Error("wiki status: implementation lands in Unit 6");
}
