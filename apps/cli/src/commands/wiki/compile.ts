/**
 * `thinkwork wiki compile` runner. Scaffolded here in Unit 2; full
 * implementation lands in Unit 3.
 */

import type { WikiCliOptions } from "./helpers.js";

export async function runWikiCompile(_opts: WikiCliOptions): Promise<void> {
	throw new Error("wiki compile: implementation lands in Unit 3");
}
